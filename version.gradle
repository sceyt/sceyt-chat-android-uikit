import com.sceyt.chat.Config

ext {
    // MavenCentral
    maven_central_publish_group = Config.mavenCentralGroup
    maven_central_atifact_id = Config.mavenCentralArtifactId
    maven_central_version = Config.mavenCentralVersion
}